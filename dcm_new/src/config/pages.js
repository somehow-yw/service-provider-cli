import basicConfig from './basic'
let pageUri = basicConfig.pageUri;

let pages = {
  // 找货
  search : `${pageUri}search`,
  // 进货单
  car : `${pageUri}car`,
  order : `${pageUri}order`

}

export default pages
